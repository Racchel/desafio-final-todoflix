import { colors } from './colors'
import { useBreakpoints } from './breakpoints'
import { font } from './font'

/**
 * xs = eXtremely small
 * sm = small. 
 * md = medium. 
 * lg = large. 
 * xl = eXtremely large
 * v = variation. 
 * default = unique
 */
export const theme = {
  colors,
  useBreakpoints,
  font
}
import { font } from './fonts'
import { colors } from './colors'
import { bk } from './breakpoints'
import { GlobalStyle } from './globalStyle'

/**
 * xs = eXtremely small
 * sm = small. 
 * md = medium. 
 * lg = large. 
 * xl = eXtremely large
 * v = variation. 
 * default = unique
 */
export const Theme = {
  bk,
  font,
  colors,
  GlobalStyle
}